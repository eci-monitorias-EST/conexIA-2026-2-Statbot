# assets

Coloca aquí el logo de la universidad con el nombre `logo.png` (fondo transparente,
recomendado ~200-300 px de alto). `statbot.py` lo carga automáticamente desde
`assets/logo.png`; si el archivo no existe, la app muestra un banner de reemplazo.
